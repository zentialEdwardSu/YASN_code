namespace YASN.Core
{
    /// <summary>
    /// Defines the z-order mode used when opening a note window.
    /// </summary>
    public enum WindowLevel
    {
        Normal,
        TopMost,
        BottomMost
    }
}
