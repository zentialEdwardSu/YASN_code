<Window x:Class="YASN.FloatingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Floating Window"
        Height="460"
        Width="350"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        ResizeMode="CanResizeWithGrip"
        Loaded="Window_Loaded"
        Activated="Window_Activated"
        Deactivated="Window_Deactivated">

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="40"
                            GlassFrameThickness="-1"
                            ResizeBorderThickness="15"
                            UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>

    <Window.Resources>
        <Style x:Key="MinimalScrollBarThumb" TargetType="Thumb">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border Background="#80FFFFFF" CornerRadius="3"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid>
                            <Track x:Name="PART_Track" IsDirectionReversed="True" Width="6">
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource MinimalScrollBarThumb}"/>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="MarkdownTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="Consolas, Microsoft YaHei UI"/>
            <Setter Property="Padding" Value="15,8,15,15"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="AcceptsReturn" Value="True"/>
            <Setter Property="AcceptsTab" Value="True"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>

        <Style x:Key="TitleBarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontFamily" Value="Segoe UI Symbol"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="shell:WindowChrome.IsHitTestVisibleInChrome" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#20000000"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#30000000"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CloseButtonStyle" TargetType="Button" BasedOn="{StaticResource TitleBarButtonStyle}">
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E74C3C"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#C0392B"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LevelButtonStyle" TargetType="Button" BasedOn="{StaticResource TitleBarButtonStyle}">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>

        <Style x:Key="IconTitleBarButtonStyle" TargetType="Button" BasedOn="{StaticResource TitleBarButtonStyle}">
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Normal"/>
        </Style>

        <Style x:Key="MarkdownActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#55FFFFFF"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,2"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Height" Value="22"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#75FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#95FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="MarkdownActionIconButtonStyle"
               TargetType="Button"
               BasedOn="{StaticResource MarkdownActionButtonStyle}">
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Width" Value="30"/>
            <Setter Property="Padding" Value="0"/>
        </Style>

        <Storyboard x:Key="CollapseEditBar">
            <DoubleAnimation Storyboard.TargetName="TitleBar"
                           Storyboard.TargetProperty="Height"
                           To="0" Duration="0:0:0.2"/>
            <DoubleAnimation Storyboard.TargetName="TitleBar"
                           Storyboard.TargetProperty="Opacity"
                           To="0" Duration="0:0:0.2"/>
            <DoubleAnimation Storyboard.TargetName="MarkdownToolbar"
                           Storyboard.TargetProperty="Height"
                           To="0" Duration="0:0:0.2"/>
            <DoubleAnimation Storyboard.TargetName="MarkdownToolbar"
                           Storyboard.TargetProperty="Opacity"
                           To="0" Duration="0:0:0.2"/>
        </Storyboard>

        <Storyboard x:Key="ExpandEditBar">
            <DoubleAnimation Storyboard.TargetName="TitleBar"
                           Storyboard.TargetProperty="Height"
                           To="40" Duration="0:0:0.2"/>
            <DoubleAnimation Storyboard.TargetName="TitleBar"
                           Storyboard.TargetProperty="Opacity"
                           To="1" Duration="0:0:0.2"/>
            <DoubleAnimation Storyboard.TargetName="MarkdownToolbar"
                           Storyboard.TargetProperty="Height"
                           To="30" Duration="0:0:0.2"/>
            <DoubleAnimation Storyboard.TargetName="MarkdownToolbar"
                           Storyboard.TargetProperty="Opacity"
                           To="1" Duration="0:0:0.2"/>
        </Storyboard>
    </Window.Resources>

    <Border x:Name="MainBorder"
            BorderThickness="1"
            CornerRadius="8"
            Background="#C81E1E1E"
            BorderBrush="#60808080"
            MouseEnter="MainBorder_MouseEnter"
            MouseLeave="MainBorder_MouseLeave">
        <Grid>
            <Border x:Name="BackgroundImageBorder"
                    CornerRadius="8"
                    Opacity="0.15">
                <Border.Background>
                    <ImageBrush x:Name="BackgroundImageBrush"
                                Stretch="UniformToFill"
                                AlignmentX="Center"
                                AlignmentY="Center"/>
                </Border.Background>
            </Border>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border x:Name="TitleBar"
                        Grid.Row="0"
                        Height="40"
                        Background="#E6D4C5E0"
                        CornerRadius="5,5,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Button Grid.Column="0" Content="&#xE712;" ToolTip="More" Style="{StaticResource IconTitleBarButtonStyle}" Click="MoreOptions_Click"/>
                        <Border Grid.Column="1" Background="Transparent" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                            <TextBlock x:Name="StatusText"
                                       Text="Note #1"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Center"
                                       Foreground="#2C3E50"
                                       FontSize="13"
                                       FontWeight="SemiBold"/>
                        </Border>
                        <StackPanel Grid.Column="2" Orientation="Horizontal">
                            <Button x:Name="EditorModeButton" Content="&#xE70F;" ToolTip="Switch Editor Mode" Style="{StaticResource IconTitleBarButtonStyle}" Click="EditorModeButton_Click"/>
                            <Button x:Name="ThemeToggleButton" Content="&#xE706;" ToolTip="Switch Theme" Style="{StaticResource IconTitleBarButtonStyle}" Click="ThemeToggle_Click"/>
                            <Button x:Name="PinTopButton" Content="&#xE718;" ToolTip="Pin Top" Style="{StaticResource IconTitleBarButtonStyle}" Click="PinButton_Click"/>
                            <Button x:Name="PinBottomButton" Content="&#xE74B;" ToolTip="To Bottom" Style="{StaticResource IconTitleBarButtonStyle}" Click="SendToBottom_Click"/>
                            <Button x:Name="MinimizeButton" Content="&#xE921;" ToolTip="Minimize" Style="{StaticResource IconTitleBarButtonStyle}" Click="MinimizeButton_Click"/>
                            <Button Content="&#xE8BB;" ToolTip="Close" Style="{StaticResource CloseButtonStyle}" Click="CloseButton_Click"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <Border x:Name="MarkdownToolbar"
                        Grid.Row="1"
                        Height="30"
                        Background="#36000000"
                        Padding="6,4">
                    <DockPanel LastChildFill="False">
                        <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                            <Button Content="&#xE91B;" ToolTip="Insert Image" Style="{StaticResource MarkdownActionIconButtonStyle}" Click="InsertImage_Click"/>
                            <Button Content="&#xE723;" ToolTip="Insert Attachment" Style="{StaticResource MarkdownActionIconButtonStyle}" Click="InsertAttachment_Click"/>
                            <Button Content="&#xE72C;" ToolTip="Refresh Preview" Style="{StaticResource MarkdownActionIconButtonStyle}" Click="RefreshPreview_Click"/>
                        </StackPanel>
                    </DockPanel>
                </Border>

                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition x:Name="EditorColumn" Width="*"/>
                        <ColumnDefinition x:Name="SplitterColumn" Width="5"/>
                        <ColumnDefinition x:Name="PreviewColumn" Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBox x:Name="ContentTextBox"
                             Grid.Column="0"
                             Style="{StaticResource MarkdownTextBoxStyle}"
                             TextChanged="ContentTextBox_TextChanged"
                             PreviewDragOver="ContentTextBox_PreviewDragOver"
                             PreviewDrop="ContentTextBox_PreviewDrop"/>

                    <GridSplitter x:Name="EditorPreviewSplitter"
                                  Grid.Column="1"
                                  Width="5"
                                  HorizontalAlignment="Stretch"
                                  VerticalAlignment="Stretch"
                                  Background="#33000000"/>

                    <Border x:Name="PreviewContainer"
                            Grid.Column="2"
                            Margin="0"
                            CornerRadius="8"
                            Background="Transparent"
                            BorderBrush="Transparent"
                            BorderThickness="0"
                            ClipToBounds="True"
                            SizeChanged="PreviewContainer_SizeChanged"
                            PreviewMouseRightButtonDown="PreviewSurface_PreviewMouseRightButtonDown">
                        <wv2:WebView2 x:Name="PreviewWebView"
                                      Margin="0"
                                      SizeChanged="PreviewWebView_SizeChanged"
                                      PreviewMouseRightButtonDown="PreviewSurface_PreviewMouseRightButtonDown"/>
                    </Border>
                </Grid>

                <Thumb x:Name="ResizeThumb"
                       Grid.Row="2"
                       Width="16"
                       Height="16"
                       Margin="0,0,2,2"
                       HorizontalAlignment="Right"
                       VerticalAlignment="Bottom"
                       Cursor="SizeNWSE"
                       Opacity="0.7"
                       shell:WindowChrome.IsHitTestVisibleInChrome="True"
                       DragDelta="ResizeThumb_DragDelta">
                    <Thumb.Template>
                        <ControlTemplate TargetType="Thumb">
                            <Grid Background="Transparent">
                                <Path Data="M 16,10 L 10,16 M 16,6 L 6,16 M 16,2 L 2,16"
                                      Stroke="#99FFFFFF"
                                      StrokeThickness="1"/>
                            </Grid>
                        </ControlTemplate>
                    </Thumb.Template>
                </Thumb>
            </Grid>
        </Grid>
    </Border>
</Window>
